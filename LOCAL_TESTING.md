# Local full-stack testing with the storefront

See the backend repo **`LOCAL_TESTING.md`** (`4M-web-backend/LOCAL_TESTING.md`) for the full checklist (Railway DB + S3 + two terminals).

**Quick start:** copy **`.env.local.example`** → **`.env.local`**, ensure **`API_INTERNAL_URL=http://127.0.0.1:3000`**, run **`npm run dev`**, open **`/admin/login`**.
